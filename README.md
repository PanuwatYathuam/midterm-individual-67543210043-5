# Student Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** [ภาษุวัฒน์ ยาท้วม]
- **Student ID:** [67543210043-5]
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
```text
midterm-individual-[67543210043-5]/
├── src/
│   ├── presentation/              # Layer 1: Presentation (HTTP Handling)
│   │   ├── controllers/           # Handle Requests & Responses
│   │   ├── routes/                # Define Endpoints
│   │   └── middlewares/           # Error Handling
│   ├── business/                  # Layer 2: Business Logic
│   │   ├── services/              # Core Logic & Rules
│   │   └── validators/            # Data Validation
│   └── data/                      # Layer 3: Data Access
│       ├── repositories/          # Database Operations (SQL)
│       └── database/              # DB Connection
├── server.js                      # Entry Point
├── students.db                    # SQLite Database
└── README.md                      # Documentation

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
- Code ยุ่งเหยิง (Spaghetti Code): โค้ดทั้งหมดกว่า 450 บรรทัดรวมอยู่ในไฟล์เดียว (server.js) ทำให้ยากต่อการอ่านและแก้ไข
- ไม่มีการแยกส่วนรับผิดชอบ (No Separation of Concerns): Business Logic ปนอยู่กับ SQL Query และ HTTP Response ทำให้แก้ไขจุดหนึ่งกระทบจุดอื่น
- ยากต่อการบำรุงรักษา: การเพิ่มฟีเจอร์หรือแก้ไขบั๊กทำได้ยากและเสี่ยงต่อการเกิดข้อผิดพลาดใหม่

### วิธีแก้ไขด้วย Layered Architecture:
- Presentation Layer: แยกส่วนจัดการ HTTP Request/Response ออกมา เพื่อให้รองรับ Client ได้หลากหลายโดยไม่ต้องแก้ Logic หลัก
- Business Layer: รวม Business Rules และ Validation ไว้ที่เดียว เพื่อความถูกต้องของข้อมูลและนำกลับมาใช้ซ้ำได้
- Data Layer: จัดการคำสั่ง SQL แยกต่างหาก ทำให้เปลี่ยน Database ได้ง่ายขึ้นในอนาคตและไม่กระทบ Logic

### ประโยชน์ที่ได้รับ:
- Maintainability: โค้ดเป็นระเบียบ อ่านง่าย แก้ไขง่ายขึ้นมาก
- Scalability: สามารถขยายระบบได้ง่าย รองรับการทำงานที่ซับซ้อนขึ้น
- Collaboration: ทีมงานสามารถแบ่งงานกันทำตาม Layer ได้โดยไม่เกิด Conflict กันบ่อยๆ

## 🚀 How to Run

\`\`\`bash
# 1. Clone repository
git clone git@github.com:PanuwatYathuam/midterm-individual-67543210043-5.git

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000/api/students
\`\`\`

## 📝 API Endpoints
GET | /api/students |	ดึงข้อมูลนักศึกษาทั้งหมด (รองรับ query ?major และ ?status)
GET |	/api/students/:id |	ดึงข้อมูลนักศึกษาตาม ID
POST |	/api/students |	เพิ่มข้อมูลนักศึกษาใหม่
PUT |	/api/students/:id |	อัปเดตข้อมูลนักศึกษาทั้งรายการ (Full Update)
PATCH |	/api/students/:id/gpa |	อัปเดตเฉพาะเกรดเฉลี่ย (GPA)
PATCH |	/api/students/:id/status |	อัปเดตสถานะ (Status)
DELETE |	/api/students/:id |	ลบข้อมูลนักศึกษา (เฉพาะสถานะที่ไม่ใช่ Active)
